import React from "react";
import HomePage from "./HomePage";

/**
 * Route /cards/:id
 * Reuses the unified desktop two-column and mobile detail view from HomePage
 */
export default function CardPage() {
  return <HomePage />;
}
